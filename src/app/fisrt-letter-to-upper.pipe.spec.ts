import { FisrtLetterToUpperPipe } from './fisrt-letter-to-upper.pipe';

describe('FisrtLetterToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new FisrtLetterToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
